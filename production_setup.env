# ==========================================
# PRODUCTION ENVIRONMENT CONFIGURATION
# ==========================================
# This file serves as a master reference for setting up your production environment.
# Copy the variables below to their respective platforms (Render & Vercel).

# ------------------------------------------
# [BACKEND] Render Configuration
# ------------------------------------------
# Domain: https://devpulse-backend.onrender.com (Example)

# 1. Django Security
SECRET_KEY=your-long-random-secret-key-here-must-be-kept-secret
DEBUG=False

# 2. Database (MongoDB Atlas)
MONGO_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/devpulse?retryWrites=true&w=majority

# 3. Domain & CORS Settings (Connecting to Frontend)
# ALLOWED_HOSTS: The domain where your Backend is hosted (Render URL)
ALLOWED_HOSTS=devpulse-backend.onrender.com

# CORS_ALLOWED_ORIGINS: The domain where your Frontend is hosted (Vercel URL)
# This allows the Frontend to make API calls to the Backend.
CORS_ALLOW_ALL_ORIGINS=False
CORS_ALLOWED_ORIGINS=https://devpulse-frontend.vercel.app
CSRF_TRUSTED_ORIGINS=https://devpulse-frontend.vercel.app

# 4. Payment Gateway (Razorpay)
RAZORPAY_KEY_ID=rzp_live_xxxxxxxxxxxxxxxx
RAZORPAY_KEY_SECRET=yyyyyyyyyyyyyyyyyyyyyyyy


# ------------------------------------------
# [FRONTEND] Vercel Configuration
# ------------------------------------------
# Domain: https://devpulse-frontend.vercel.app (Example)

# 1. API Connection
# This tells the Frontend where to send requests (Your Render Backend URL)
NEXT_PUBLIC_API_URL=https://devpulse-backend.onrender.com

# ==========================================
# NOTES
# ==========================================
# - Ensure no trailing slashes in URLs for CORS settings.
# - On Render, add these under the "Environment" tab.
# - On Vercel, add these under "Settings" -> "Environment Variables".
